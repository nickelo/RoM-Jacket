goto :%CRCSRC%
exit /b
:cd5cf5a4
for /f "%FLDSWTCH%" %%a in ("apple1basic.wav:Apple I Basic") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:1914635e
for /f "%FLDSWTCH%" %%a in ("apple1bas.wav:Apple I Basic (Alt)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:21d4f61d
for /f "%FLDSWTCH%" %%a in ("adventure.wav:Adventure") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:098614ca
for /f "%FLDSWTCH%" %%a in ("applesoft lite.wav:Applesoft Lite") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:eec88347
for /f "%FLDSWTCH%" %%a in ("apple30th.wav:Apple 30th") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:72a2127c
for /f "%FLDSWTCH%" %%a in ("asmmchess.wav:ASMmchess") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f461367a
for /f "%FLDSWTCH%" %%a in ("codebreaker.wav:Codebreaker") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:06af2136
for /f "%FLDSWTCH%" %%a in ("life.wav:Life") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3c87f6cd
for /f "%FLDSWTCH%" %%a in ("lunar lander.wav:Lunar Lander") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b44936a4
for /f "%FLDSWTCH%" %%a in ("mastermind.wav:Mastermind") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9b3ca487
for /f "%FLDSWTCH%" %%a in ("microchess.wav:Microchess") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:84c241ce
for /f "%FLDSWTCH%" %%a in ("pasart.wav:Pasart") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:296a327d
for /f "%FLDSWTCH%" %%a in ("typingtutor.wav:TypingTutor") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:793422b8
for /f "%FLDSWTCH%" %%a in ("aceyducey.wav:AceyDucey") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:faed6233
for /f "%FLDSWTCH%" %%a in ("applei enhanced checkers.wav:Apple I Enhanced Checkers") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7e230c17
for /f "%FLDSWTCH%" %%a in ("blackjack.wav:Blackjack") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:22383368
for /f "%FLDSWTCH%" %%a in ("bowling.wav:Bowling") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2c040f8e
for /f "%FLDSWTCH%" %%a in ("buzzword.wav:Buzzword") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6122e49e
for /f "%FLDSWTCH%" %%a in ("columns.wav:Columns") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:30f252f4
for /f "%FLDSWTCH%" %%a in ("concentration.wav:Concentration") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d9675f54
for /f "%FLDSWTCH%" %%a in ("craps.wav:Craps") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:28ad3585
for /f "%FLDSWTCH%" %%a in ("deal or no deal.wav:Deal or No Deal") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c8cf353e
for /f "%FLDSWTCH%" %%a in ("eliza.wav:ELIZA") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:12613822
for /f "%FLDSWTCH%" %%a in ("football.wav:Football") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9760bd93
for /f "%FLDSWTCH%" %%a in ("hamurabi.wav:Hamurabi") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d2926fb9
for /f "%FLDSWTCH%" %%a in ("hangman.wav:Hangman") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:21024b5d
for /f "%FLDSWTCH%" %%a in ("hilo.wav:HiLo") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:becc6aff
for /f "%FLDSWTCH%" %%a in ("lunarlander (ascii graphics).wav:Lunar Lander (ASCII Graphics)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6d2221be
for /f "%FLDSWTCH%" %%a in ("matrix.wav:Matrix") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:dd90caea
for /f "%FLDSWTCH%" %%a in ("slots.wav:Slots") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:42d7ff61
for /f "%FLDSWTCH%" %%a in ("startrek.wav:Star Trek") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:659d91a9
for /f "%FLDSWTCH%" %%a in ("startrek2003.wav:Star Trek 2003") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4a3d6dde
for /f "%FLDSWTCH%" %%a in ("stopwatch.wav:Stopwatch") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3d9f5163
for /f "%FLDSWTCH%" %%a in ("trek ii.wav:TREK II") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:1330687d
for /f "%FLDSWTCH%" %%a in ("wordcross.wav:WordCross") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:83cb19a5
for /f "%FLDSWTCH%" %%a in ("wumpus.wav:Wumpus") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:83cb19a5
for /f "%FLDSWTCH%" %%a in ("wumpus.wav:Wumpus") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
