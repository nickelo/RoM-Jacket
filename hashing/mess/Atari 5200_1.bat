goto :%CRCSRC%
exit /b
:0de2db48
for /f "%FLDSWTCH%" %%a in ("5200menu.bin:5200 Menu (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:35484751
for /f "%FLDSWTCH%" %%a in ("aep.bin:A.E. (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f43e7cd0
for /f "%FLDSWTCH%" %%a in ("decathln.bin:The Activision Decathlon") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:38480891
for /f "%FLDSWTCH%" %%a in ("asteroid.bin:Asteroids (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4019ecec
for /f "%FLDSWTCH%" %%a in ("astrchse.bin:Astro Chase") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:94d97d14
for /f "%FLDSWTCH%" %%a in ("ballblze.bin:Ballblazer") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:21d19c8f
for /f "%FLDSWTCH%" %%a in ("barbball.bin:Barroom Baseball (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b3b8e314
for /f "%FLDSWTCH%" %%a in ("bzone.bin:Battlezone (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9bae58dc
for /f "%FLDSWTCH%" %%a in ("beamrid.bin:Beamrider") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:be3cd348
for /f "%FLDSWTCH%" %%a in ("berzerk.bin:Berzerk") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ed47b0d8
for /f "%FLDSWTCH%" %%a in ("blckbelt.bin:Black Belt (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c8f9c094
for /f "%FLDSWTCH%" %%a in ("blaster.bin:Blaster (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0624e6e7
for /f "%FLDSWTCH%" %%a in ("blueprnt.bin:Blue-Print") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7873c6dd
for /f "%FLDSWTCH%" %%a in ("bbstrksb.bin:Bounty Bob Strikes Back!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:!:04807705
for /f "%FLDSWTCH%" %%a in ("buckrog.bin:Buck Rogers - Planet of Zoom") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:536a70fe
for /f "%FLDSWTCH%" %%a in ("centiped.bin:Centipede") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9ad53bbc
for /f "%FLDSWTCH%" %%a in ("choplift.bin:Choplifter!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f1f42bbd
for /f "%FLDSWTCH%" %%a in ("congo.bin:Congo Bongo") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:fd541c80
for /f "%FLDSWTCH%" %%a in ("cntrmsre.bin:Countermeasure") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4b910461
for /f "%FLDSWTCH%" %%a in ("pitfall2.bin:David Crane's Pitfall II - Lost Caverns") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bd52623b
for /f "%FLDSWTCH%" %%a in ("defender.bin:Defender") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6a687f9c
for /f "%FLDSWTCH%" %%a in ("digdug.bin:Dig Dug") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:460def2d
for /f "%FLDSWTCH%" %%a in ("dredfctr.bin:The Dreadnaught Factor") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d3bd3221
for /f "%FLDSWTCH%" %%a in ("finalleg.bin:Final Legacy (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:04b299a4
for /f "%FLDSWTCH%" %%a in ("friskyt.bin:Frisky Tom (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ae7e3444
for /f "%FLDSWTCH%" %%a in ("frogger.bin:Frogger") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0af19345
for /f "%FLDSWTCH%" %%a in ("frogger2.bin:Frogger II- Threeedeep!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3ef4a23f
for /f "%FLDSWTCH%" %%a in ("galaxian.bin:Galaxian") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e955db74
for /f "%FLDSWTCH%" %%a in ("gorf.bin:Gorf") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:063ec2c4
for /f "%FLDSWTCH%" %%a in ("gremlins.bin:Gremlins") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:cfd4a7f9
for /f "%FLDSWTCH%" %%a in ("gyruss.bin:Gyruss") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:18a73af3
for /f "%FLDSWTCH%" %%a in ("hero.bin:H.E.R.O.") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d9ae4518
for /f "%FLDSWTCH%" %%a in ("jamesbnd.bin:James Bond") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bfd30c01
for /f "%FLDSWTCH%" %%a in ("joust.bin:Joust") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:59983c40
for /f "%FLDSWTCH%" %%a in ("jrpacman.bin:Jr Pacman (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2c676662
for /f "%FLDSWTCH%" %%a in ("jungleh.bin:Jungle Hunt") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ee702214
for /f "%FLDSWTCH%" %%a in ("krazysht.bin:K-Razy Shoot Out") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:420f5d0b
for /f "%FLDSWTCH%" %%a in ("kaboom.bin:Kaboom!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ecfa624f
for /f "%FLDSWTCH%" %%a in ("kangaroo.bin:Kangaroo") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:8fe3bb2c
for /f "%FLDSWTCH%" %%a in ("keystone.bin:Keystone Kapers") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:83517703
for /f "%FLDSWTCH%" %%a in ("laststar.bin:The Last Starfighter (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:84df4925
for /f "%FLDSWTCH%" %%a in ("loontoon.bin:Looney Tunes Hotel (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:873742f1
for /f "%FLDSWTCH%" %%a in ("mario.bin:Mario Bros.") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9fb13411
for /f "%FLDSWTCH%" %%a in ("meebzork.bin:Meebzork (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:240a1e1a
for /f "%FLDSWTCH%" %%a in ("megamnia.bin:MegaMania") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ab8e035b
for /f "%FLDSWTCH%" %%a in ("meteorit.bin:Meteorites") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:931a454a
for /f "%FLDSWTCH%" %%a in ("microgam.bin:Microgammon SB (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:969cfe1a
for /f "%FLDSWTCH%" %%a in ("milliped.bin:Millipede (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7df1adfb
for /f "%FLDSWTCH%" %%a in ("mine2049.bin:Miner 2049er") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c597c087
for /f "%FLDSWTCH%" %%a in ("minigolf.bin:Miniature Golf (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:44d3ff6f
for /f "%FLDSWTCH%" %%a in ("missile.bin:Missile Command") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2a640143
for /f "%FLDSWTCH%" %%a in ("montezum.bin:Montezuma's Revenge") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d0b2f285
for /f "%FLDSWTCH%" %%a in ("mpatrol.bin:Moon Patrol") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0f24243c
for /f "%FLDSWTCH%" %%a in ("mntnking.bin:Mountain King") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:aa55f9be
for /f "%FLDSWTCH%" %%a in ("docastle.bin:Mr. Do!'s Castle") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:752f5efd
for /f "%FLDSWTCH%" %%a in ("mspacman.bin:Ms. Pac-Man") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:8873ef51
for /f "%FLDSWTCH%" %%a in ("pacman.bin:Pac-Man") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e4f8ba8c
for /f "%FLDSWTCH%" %%a in ("pengo.bin:Pengo") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b2887833
for /f "%FLDSWTCH%" %%a in ("pitfall.bin:Pitfall!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:abc2d1e4
for /f "%FLDSWTCH%" %%a in ("polepos.bin:Pole Position") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a18a9a40
for /f "%FLDSWTCH%" %%a in ("popeye.bin:Popeye") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3fe4a401
for /f "%FLDSWTCH%" %%a in ("qbert.bin:Q-bert") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:aea6d2c2
for /f "%FLDSWTCH%" %%a in ("qix.bin:Qix") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b5f3402b
for /f "%FLDSWTCH%" %%a in ("questroo.bin:Quest for Quintana Roo") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:!:44166592
for /f "%FLDSWTCH%" %%a in ("rsbsebll.bin:RealSports Baseball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:dd217276
for /f "%FLDSWTCH%" %%a in ("rsbktbll.bin:RealSports Basketball (19831216)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c90196fa
for /f "%FLDSWTCH%" %%a in ("rsbktbll1.bin:RealSports Basketball (Prototype, 19831031)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0f996184
for /f "%FLDSWTCH%" %%a in ("rsbktbll2.bin:RealSports Basketball (Prototype, 19821105)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4336c2cc
for /f "%FLDSWTCH%" %%a in ("rsftball.bin:RealSports Football") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ecbd1853
for /f "%FLDSWTCH%" %%a in ("rssoccer.bin:RealSports Soccer") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:10f33c90
for /f "%FLDSWTCH%" %%a in ("rstennis.bin:RealSports Tennis") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:762c591b
for /f "%FLDSWTCH%" %%a in ("fractal.bin:Rescue on Fractalus!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:09fc7648
for /f "%FLDSWTCH%" %%a in ("riveraid.bin:River Raid") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a97606ab
for /f "%FLDSWTCH%" %%a in ("roadrun.bin:Road Runner (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4252abd9
for /f "%FLDSWTCH%" %%a in ("robotron.bin:Robotron- 2084") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b68d61e8
for /f "%FLDSWTCH%" %%a in ("spcedngn.bin:Space Dungeon") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:de5c354a
for /f "%FLDSWTCH%" %%a in ("spaceinv.bin:Space Invaders") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:387365dc
for /f "%FLDSWTCH%" %%a in ("spceshut.bin:Space Shuttle - A Journey Into Space") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3c311303
for /f "%FLDSWTCH%" %%a in ("spitfire.bin:Spitfire (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:73b5b6fb
for /f "%FLDSWTCH%" %%a in ("sprtgoof.bin:Sport Goofy (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7d819a9f
for /f "%FLDSWTCH%" %%a in ("starraid.bin:Star Raiders") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:69f23548
for /f "%FLDSWTCH%" %%a in ("startrek.bin:Star Trek") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0675f0a5
for /f "%FLDSWTCH%" %%a in ("starwars.bin:Star Wars - Return of the Jedi - Death Star Battle") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:75f566df
for /f "%FLDSWTCH%" %%a in ("swa.bin:Star Wars - The Arcade Game") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:1d1cee27
for /f "%FLDSWTCH%" %%a in ("stargate.bin:Stargate (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a0642110
for /f "%FLDSWTCH%" %%a in ("sprbreak.bin:Super Breakout") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:97debcd2
for /f "%FLDSWTCH%" %%a in ("scobra.bin:Super Cobra") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0a4ddb1e
for /f "%FLDSWTCH%" %%a in ("spacman.bin:Super Pac-Man (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:1187342f
for /f "%FLDSWTCH%" %%a in ("tempest.bin:Tempest (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0ba22ece
for /f "%FLDSWTCH%" %%a in ("trackfld.bin:Track and Field (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:caaea0a4
for /f "%FLDSWTCH%" %%a in ("vanguard.bin:Vanguard") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d6f7ddfd
for /f "%FLDSWTCH%" %%a in ("wow.bin:Wizard of Wor") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b8faaec3
for /f "%FLDSWTCH%" %%a in ("xariarna.bin:Xari Arena (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:382634dc
for /f "%FLDSWTCH%" %%a in ("xevious.bin:Xevious (Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f47bc091
for /f "%FLDSWTCH%" %%a in ("yllowsub.bin:Yellow Sub Demo") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:741746d1
for /f "%FLDSWTCH%" %%a in ("zaxxon.bin:Zaxxon") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:da228530
for /f "%FLDSWTCH%" %%a in ("zenji.bin:Zenji") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2959d827
for /f "%FLDSWTCH%" %%a in ("znerangr.bin:Zone Ranger") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:28278cd6
for /f "%FLDSWTCH%" %%a in ("petetest.bin:PAM- Pete's Test (USA)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e8b130c4
for /f "%FLDSWTCH%" %%a in ("pamdiag2.bin:PAM Diag 2.0") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ce07d9ad
for /f "%FLDSWTCH%" %%a in ("pamdg23.bin:Atari PAM Diagnostics (Rev 2.3)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7ea86e87
for /f "%FLDSWTCH%" %%a in ("finaltst.bin:Atari PAM Final System Test (Rev 1.0)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3bd5fdd6
for /f "%FLDSWTCH%" %%a in ("boogie.bin:Boogie (Demo)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7c988054
for /f "%FLDSWTCH%" %%a in ("cblast.bin:Castle Blast") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d50e4061
for /f "%FLDSWTCH%" %%a in ("ccrisis.bin:Castle Crisis") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:917be656
for /f "%FLDSWTCH%" %%a in ("koffiyk.bin:Koffi- Yellow Kopter") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:917be656
for /f "%FLDSWTCH%" %%a in ("koffiyk.bin:Koffi- Yellow Kopter") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
