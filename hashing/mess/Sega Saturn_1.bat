goto :%CRCSRC%
exit /b
:90412e10
for /f "%FLDSWTCH%" %%a in ("kof95 (je).rom:The King of Fighters '95 (Euro, Jpn) (Support Cartridge)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:
for /f "%FLDSWTCH%" %%a in ("ultraman (j).rom:Ultraman (Jpn) (Support Cartridge)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:17031c7a
for /f "%FLDSWTCH%" %%a in ("sega saturn test menu v1.0f.bin:Test Menu v1.0f") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:cc518d69
for /f "%FLDSWTCH%" %%a in ("action replay rom.bin:Action Replay") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:cc518d69
for /f "%FLDSWTCH%" %%a in ("action replay rom.bin:Action Replay") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
