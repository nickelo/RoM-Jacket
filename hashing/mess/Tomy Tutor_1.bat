goto :%CRCSRC%
exit /b
:e6a7b204
for /f "%FLDSWTCH%" %%a in ("bombman.bin:Bombman") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6e9b98c7
for /f "%FLDSWTCH%" %%a in ("crzracer.bin:Car-azy-racer") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f3059779
for /f "%FLDSWTCH%" %%a in ("crawlers.bin:Cave Crawlers") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f3c61b7d
for /f "%FLDSWTCH%" %%a in ("deepsix.bin:Deep Six") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:fffd897e
for /f "%FLDSWTCH%" %%a in ("frogger.bin:Frogger") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:87bc1c05
for /f "%FLDSWTCH%" %%a in ("hyperspc.bin:Hyperspace") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ad635119
for /f "%FLDSWTCH%" %%a in ("jungler.bin:Jungler") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:1f652f42
for /f "%FLDSWTCH%" %%a in ("lmotion.bin:Loco-motion") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:398b2321
for /f "%FLDSWTCH%" %%a in ("misattck.bin:Mission Attack") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2af704f8
for /f "%FLDSWTCH%" %%a in ("mnstrinn.bin:Monster Inn") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:db469444
for /f "%FLDSWTCH%" %%a in ("nightfli.bin:Night Flight") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:05396699
for /f "%FLDSWTCH%" %%a in ("pooyan.bin:Pooyan") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:87085401
for /f "%FLDSWTCH%" %%a in ("saurus.bin:Saurusland") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:cc9901d1
for /f "%FLDSWTCH%" %%a in ("scramble.bin:Scramble (USA)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bd708ab4
for /f "%FLDSWTCH%" %%a in ("scramblej.bin:Scramble (Jpn)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:95577ceb
for /f "%FLDSWTCH%" %%a in ("torpedo.bin:Torpedo Terror") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c0c78141
for /f "%FLDSWTCH%" %%a in ("trafjam.bin:Traffic Jam") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c970f2c3
for /f "%FLDSWTCH%" %%a in ("trafjama.bin:Traffic Jam (Alt)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d00210df
for /f "%FLDSWTCH%" %%a in ("turpin.bin:Turpin") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d00210df
for /f "%FLDSWTCH%" %%a in ("turpin.bin:Turpin") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
