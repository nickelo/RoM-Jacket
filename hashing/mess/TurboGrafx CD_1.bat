goto :%CRCSRC%
exit /b
:b03e0b32
for /f "%FLDSWTCH%" %%a in ("aero blasters (usa).pce:Aero Blasters") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:933d5bcc
for /f "%FLDSWTCH%" %%a in ("air zonk (usa).pce:Air Zonk") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ea488494
for /f "%FLDSWTCH%" %%a in ("alien crush (usa).pce:Alien Crush") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:420fa189
for /f "%FLDSWTCH%" %%a in ("ballistix (usa).pce:Ballistix") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e70b01af
for /f "%FLDSWTCH%" %%a in ("battle royale (usa).pce:Battle Royale") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b4a1b0f6
for /f "%FLDSWTCH%" %%a in ("blazing lazers (usa).pce:Blazing Lazers") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:37baf6bc
for /f "%FLDSWTCH%" %%a in ("bloody wolf (usa).pce:Bloody Wolf") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:56171c1c
for /f "%FLDSWTCH%" %%a in ("bomberman '93 (usa).pce:Bomberman '93") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:5f6f3c2a
for /f "%FLDSWTCH%" %%a in ("bomberman (usa).pce:Bomberman") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:5a3f76d8
for /f "%FLDSWTCH%" %%a in ("bonk iii - bonk's big adventure (usa).pce:Bonk III - Bonk's Big Adventure") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:599ead9b
for /f "%FLDSWTCH%" %%a in ("bonk's adventure (usa).pce:Bonk's Adventure") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:14250f9a
for /f "%FLDSWTCH%" %%a in ("bonk's revenge (usa).pce:Bonk's Revenge") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:605be213
for /f "%FLDSWTCH%" %%a in ("boxyboy (usa).pce:Boxyboy") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:cca08b02
for /f "%FLDSWTCH%" %%a in ("bravoman (usa).pce:Bravoman") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bb0b3aef
for /f "%FLDSWTCH%" %%a in ("cadash (usa).pce:Cadash") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:15ee889a
for /f "%FLDSWTCH%" %%a in ("champions forever boxing (usa).pce:Champions Forever Boxing") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:8cd13e9a
for /f "%FLDSWTCH%" %%a in ("chew man fu (usa).pce:Chew Man Fu") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a2ee361d
for /f "%FLDSWTCH%" %%a in ("china warrior (usa).pce:China Warrior") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9033e83a
for /f "%FLDSWTCH%" %%a in ("cratermaze (usa).pce:Cratermaze") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4cfb6e3e
for /f "%FLDSWTCH%" %%a in ("cyber core (usa).pce:Cyber Core") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4ac97606
for /f "%FLDSWTCH%" %%a in ("darkwing duck (usa).pce:Darkwing Duck") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9edab596
for /f "%FLDSWTCH%" %%a in ("davis cup tennis (usa).pce:Davis Cup Tennis") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f5d98b0b
for /f "%FLDSWTCH%" %%a in ("dead moon (usa).pce:Dead Moon") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:16b40b44
for /f "%FLDSWTCH%" %%a in ("deep blue (usa).pce:Deep Blue") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:157b4492
for /f "%FLDSWTCH%" %%a in ("devil's crush - naxat pinball (usa).pce:Devil's Crush - Naxat Pinball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4a1a8c60
for /f "%FLDSWTCH%" %%a in ("double dungeons - w (usa).pce:Double Dungeons - W") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:086f148c
for /f "%FLDSWTCH%" %%a in ("dragon spirit (usa).pce:Dragon Spirit") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7d2c4b09
for /f "%FLDSWTCH%" %%a in ("dragon's curse (usa).pce:Dragon's Curse") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:fea27b32
for /f "%FLDSWTCH%" %%a in ("drop.off (usa).pce:Drop.Off") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4ff01515
for /f "%FLDSWTCH%" %%a in ("dungeon explorer (usa).pce:Dungeon Explorer") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0bc0a12b
for /f "%FLDSWTCH%" %%a in ("falcon (usa).pce:Falcon") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e8c3573d
for /f "%FLDSWTCH%" %%a in ("fantasy zone (usa).pce:Fantasy Zone") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:26408ea3
for /f "%FLDSWTCH%" %%a in ("final lap twin (usa).pce:Final Lap Twin") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2909dec6
for /f "%FLDSWTCH%" %%a in ("galaga '90 (usa).pce:Galaga '90") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2db4c1fd
for /f "%FLDSWTCH%" %%a in ("ghost manor (usa).pce:Ghost Manor") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f370b58e
for /f "%FLDSWTCH%" %%a in ("gunboat (usa).pce:Gunboat") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:8b29c3aa
for /f "%FLDSWTCH%" %%a in ("hit the ice - vhl the official video hockey league (usa).pce:Hit the Ice - VHL - The Official Video Hockey League") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e2470f5f
for /f "%FLDSWTCH%" %%a in ("impossamole (usa).pce:Impossamole") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e01c5127
for /f "%FLDSWTCH%" %%a in ("j.j. & jeff (usa).pce:J.J. & Jeff") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:83384572
for /f "%FLDSWTCH%" %%a in ("jack nicklaus' turbo golf (usa).pce:Jack Nicklaus' Turbo Golf") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9d2f6193
for /f "%FLDSWTCH%" %%a in ("jackie chan's action kung fu (usa).pce:Jackie Chan's Action Kung Fu") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:474d7a72
for /f "%FLDSWTCH%" %%a in ("keith courage in alpha zones (usa).pce:Keith Courage in Alpha Zones") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2f2e2240
for /f "%FLDSWTCH%" %%a in ("king of casino (usa).pce:King of Casino") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0f1b59b4
for /f "%FLDSWTCH%" %%a in ("klax (usa).pce:Klax") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3c131486
for /f "%FLDSWTCH%" %%a in ("legend of hero tonma (usa).pce:Legend of Hero Tonma") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:220ebf91
for /f "%FLDSWTCH%" %%a in ("legendary axe ii, the (usa).pce:The Legendary Axe II") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2d211007
for /f "%FLDSWTCH%" %%a in ("legendary axe, the (usa).pce:The Legendary Axe") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:95cd2979
for /f "%FLDSWTCH%" %%a in ("magical chase (usa).pce:Magical Chase") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:93f316f7
for /f "%FLDSWTCH%" %%a in ("military madness (usa).pce:Military Madness") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e2b0d544
for /f "%FLDSWTCH%" %%a in ("moto roader (usa).pce:Moto Roader") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a9a94e1b
for /f "%FLDSWTCH%" %%a in ("neutopia (usa).pce:Neutopia") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c4ed4307
for /f "%FLDSWTCH%" %%a in ("neutopia ii (usa).pce:Neutopia II") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:756a1802
for /f "%FLDSWTCH%" %%a in ("new adventure island (usa).pce:New Adventure Island") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c159761b
for /f "%FLDSWTCH%" %%a in ("night creatures (usa).pce:Night Creatures") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:de8af1c1
for /f "%FLDSWTCH%" %%a in ("ninja spirit (usa).pce:Ninja Spirit") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:fae0fc60
for /f "%FLDSWTCH%" %%a in ("order of the griffon (usa).pce:Order of the Griffon") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e7bf2a74
for /f "%FLDSWTCH%" %%a in ("ordyne (usa).pce:Ordyne") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d6e30ccd
for /f "%FLDSWTCH%" %%a in ("pac-land (usa).pce:Pac-land") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a980e0e9
for /f "%FLDSWTCH%" %%a in ("panza kick boxing (usa).pce:Panza Kick Boxing") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e6458212
for /f "%FLDSWTCH%" %%a in ("parasol stars - the story of bubble bobble iii (usa).pce:Parasol Stars - The Story of Bubble Bobble III") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ed1d3843
for /f "%FLDSWTCH%" %%a in ("power golf (usa).pce:Power Golf") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6cc10824
for /f "%FLDSWTCH%" %%a in ("psychosis (usa).pce:Psychosis") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:91ce5156
for /f "%FLDSWTCH%" %%a in ("r-type (usa).pce:R-Type") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bc59c31e
for /f "%FLDSWTCH%" %%a in ("raiden (usa).pce:Raiden") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:77a924b7
for /f "%FLDSWTCH%" %%a in ("samurai-ghost (usa).pce:Samurai-Ghost") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2774462c
for /f "%FLDSWTCH%" %%a in ("shockman (usa).pce:Shockman") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d1993c9f
for /f "%FLDSWTCH%" %%a in ("sidearms - hyper dyne (usa).pce:Sidearms - Hyper Dyne") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:fa7e5d66
for /f "%FLDSWTCH%" %%a in ("silent debuggers (usa).pce:Silent Debuggers") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4f6e2dbd
for /f "%FLDSWTCH%" %%a in ("sinistron (usa).pce:Sinistron") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4bb68b13
for /f "%FLDSWTCH%" %%a in ("soldier blade (usa).pce:Soldier Blade") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:8fcaf2e9
for /f "%FLDSWTCH%" %%a in ("somer assault (usa).pce:Somer Assault") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f74e5eb3
for /f "%FLDSWTCH%" %%a in ("sonic spike - world championship beach volleyball (usa).pce:Sonic Spike - World Championship Beach Volleyball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:43b05eb8
for /f "%FLDSWTCH%" %%a in ("space harrier (usa).pce:Space Harrier") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:d00ca74f
for /f "%FLDSWTCH%" %%a in ("splatterhouse (usa).pce:Splatterhouse") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:db29486f
for /f "%FLDSWTCH%" %%a in ("super star soldier (usa).pce:Super Star Soldier") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:245040b3
for /f "%FLDSWTCH%" %%a in ("super volleyball (usa).pce:Super Volleyball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9298254c
for /f "%FLDSWTCH%" %%a in ("taito chase h.q. (usa).pce:Taito Chase H.Q.") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e9d51797
for /f "%FLDSWTCH%" %%a in ("takin' it to the hoop (usa).pce:Takin' It to the Hoop") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bae9cecc
for /f "%FLDSWTCH%" %%a in ("talespin (usa).pce:TaleSpin") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:985d492d
for /f "%FLDSWTCH%" %%a in ("tiger road (usa).pce:Tiger Road") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:02c39660
for /f "%FLDSWTCH%" %%a in ("time cruise (usa).pce:Time Cruise") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:5d395019
for /f "%FLDSWTCH%" %%a in ("timeball (usa).pce:Timeball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:48e6fd34
for /f "%FLDSWTCH%" %%a in ("tricky kick (usa).pce:Tricky Kick") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:eb045edf
for /f "%FLDSWTCH%" %%a in ("turrican (usa).pce:Turrican") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ea54d653
for /f "%FLDSWTCH%" %%a in ("tv sports basketball (usa).pce:TV Sports Basketball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:5e25b557
for /f "%FLDSWTCH%" %%a in ("tv sports football (usa).pce:TV Sports Football") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:97fe5bcf
for /f "%FLDSWTCH%" %%a in ("tv sports hockey (usa).pce:TV Sports Hockey") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:99d14fb7
for /f "%FLDSWTCH%" %%a in ("veigues - tactical gladiator (usa).pce:Veigues - Tactical Gladiator") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:85cbd045
for /f "%FLDSWTCH%" %%a in ("victory run (usa).pce:Victory Run") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:79d49a0d
for /f "%FLDSWTCH%" %%a in ("vigilante (usa).pce:Vigilante") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4186d0c0
for /f "%FLDSWTCH%" %%a in ("world class baseball (usa).pce:World Class Baseball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a4457df0
for /f "%FLDSWTCH%" %%a in ("world court tennis (usa).pce:World Court Tennis") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4b93f0ac
for /f "%FLDSWTCH%" %%a in ("world sports competition (usa).pce:World Sports Competition") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3ca7db48
for /f "%FLDSWTCH%" %%a in ("yo, bro (usa).pce:Yo, Bro") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2b5b75fe
for /f "%FLDSWTCH%" %%a in ("[cd] turbografx cd super system card (usa) (v3.0).pce:Turbografx CD Super System Card (v3.0)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ff2a5ec3
for /f "%FLDSWTCH%" %%a in ("[cd] turbografx cd system card (usa) (v2.0).pce:Turbografx CD System Card (v2.0)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ff2a5ec3
for /f "%FLDSWTCH%" %%a in ("[cd] turbografx cd system card (usa) (v2.0).pce:Turbografx CD System Card (v2.0)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
