goto :%CRCSRC%
exit /b
:b2313487
for /f "%FLDSWTCH%" %%a in ("3dmnstrm.bin:3D Mine Storm") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9f62e167
for /f "%FLDSWTCH%" %%a in ("animactn.bin:Animaction") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:b7510971
for /f "%FLDSWTCH%" %%a in ("armora.bin:Armor Attack") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c8e2f247
for /f "%FLDSWTCH%" %%a in ("artmastr.bin:Art Master") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a2fa649b
for /f "%FLDSWTCH%" %%a in ("bedlam.bin:Bedlam") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:eafe2455
for /f "%FLDSWTCH%" %%a in ("berzerk.bin:Berzerk") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2adc4eaa
for /f "%FLDSWTCH%" %%a in ("berzerkd.bin:Berzerk (Debugged Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:0f11ce0c
for /f "%FLDSWTCH%" %%a in ("blitz.bin:Blitz!") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:881b27c5
for /f "%FLDSWTCH%" %%a in ("blitza.bin:Blitz! (Alt)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f1b1de78
for /f "%FLDSWTCH%" %%a in ("cleanswp.bin:Clean Sweep") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6c1afa52
for /f "%FLDSWTCH%" %%a in ("cchasm.bin:Cosmic Chasm") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:92709b11
for /f "%FLDSWTCH%" %%a in ("crzycstr.bin:Crazy Coaster") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:9dcfd5b7
for /f "%FLDSWTCH%" %%a in ("darktwer.bin:Dark Tower (Proto)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:fe9a501a
for /f "%FLDSWTCH%" %%a in ("enginean.bin:Engine Analyzer (Proto)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:13a35c8a
for /f "%FLDSWTCH%" %%a in ("fnarzod.bin:Fortress of Narzod") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:e61a7de3
for /f "%FLDSWTCH%" %%a in ("headsup.bin:Heads Up") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4bb13472
for /f "%FLDSWTCH%" %%a in ("hyprchse.bin:Hyperchase") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:05838962
for /f "%FLDSWTCH%" %%a in ("mailplne.bin:Mail Plane (Proto)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:afefa29c
for /f "%FLDSWTCH%" %%a in ("mldymstr.bin:Melody Master") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:6d509566
for /f "%FLDSWTCH%" %%a in ("minestrm.bin:Mine Storm") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:f5b3b374
for /f "%FLDSWTCH%" %%a in ("minestr2.bin:Mine Storm II") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:050bd949
for /f "%FLDSWTCH%" %%a in ("nrrwescp.bin:Narrow Escape") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c25f2e0e
for /f "%FLDSWTCH%" %%a in ("ptchduel.bin:Pitcher's Duel (Proto)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a7471784
for /f "%FLDSWTCH%" %%a in ("polarres.bin:Polar Rescue") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ad844645
for /f "%FLDSWTCH%" %%a in ("polarrsb.bin:Polar Rescue (Beta)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a00ed3d6
for /f "%FLDSWTCH%" %%a in ("polepos.bin:Pole Position") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c10f37d8
for /f "%FLDSWTCH%" %%a in ("poleposa.bin:Pole Position (Alt)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:34a98132
for /f "%FLDSWTCH%" %%a in ("ripoff.bin:Rip-Off") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:863b89e8
for /f "%FLDSWTCH%" %%a in ("rcktsldg.bin:Rocket Sledge (Sample)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:707c8ffe
for /f "%FLDSWTCH%" %%a in ("scramble.bin:Scramble") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:5c4611c8
for /f "%FLDSWTCH%" %%a in ("solarq.bin:Solar Quest") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:a3be5aff
for /f "%FLDSWTCH%" %%a in ("spacewar.bin:Space Wars") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:62a61843
for /f "%FLDSWTCH%" %%a in ("spike.bin:Spike") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:02f41733
for /f "%FLDSWTCH%" %%a in ("spinball.bin:Spinball") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:c9039623
for /f "%FLDSWTCH%" %%a in ("starcas.bin:Star Castle") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7967a8cb
for /f "%FLDSWTCH%" %%a in ("starship.bin:Star Ship") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:3768d8c3
for /f "%FLDSWTCH%" %%a in ("startrek.bin:Star Trek - The Game") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:563f846b
for /f "%FLDSWTCH%" %%a in ("startrekp.bin:Star Trek (Debugged Prototype)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:4de1f06b
for /f "%FLDSWTCH%" %%a in ("starhawk.bin:Star Hawk") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:7297e815
for /f "%FLDSWTCH%" %%a in ("testrev4.bin:Test (Rev. 4)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:bb4791f1
for /f "%FLDSWTCH%" %%a in ("tourdefr.bin:Tour De France (Proto)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:521107c3
for /f "%FLDSWTCH%" %%a in ("webwarp.bin:Web Warp") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2ee20103
for /f "%FLDSWTCH%" %%a in ("webwars.bin:Web Wars") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:2ee20103
for /f "%FLDSWTCH%" %%a in ("webwars.bin:Web Wars") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
