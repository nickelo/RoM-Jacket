:c64243bc
for /f "%FLDSWTCH%" %%a in ("prolog.bin:PROLOG interpreter (OM-51P)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:dd9cc2bd
for /f "%FLDSWTCH%" %%a in ("basic.bin:BASIC interpreter (OM-53B)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:dd9cc2bd
for /f "%FLDSWTCH%" %%a in ("basic.bin:BASIC interpreter (OM-53B)") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
