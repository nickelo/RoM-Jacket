:ca822bf0
for /f "%FLDSWTCH%" %%a in ("bootdisk.img:Boot disk") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
:ca822bf0
for /f "%FLDSWTCH%" %%a in ("bootdisk.img:Boot disk") do (SET ROMRNM=%%~a&SET ROMFLDR=%%~b)
exit /b
